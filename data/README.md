# Bundled datasets

| File | Description |
|------|-------------|
| `matrixBinaria-Global-100-p.csv` | Small binary-feature example (embedded in-repo). Target column: `class`, positive label: `p`. |
| `alon-clean50-pn-width-2.CSV` | Alon leukemia gene expression benchmark (optional; ~900 KB). Target column: **`y`**, positive label: **`p`**. |

If `alon-clean50-pn-width-2.CSV` is not present, extract it from **`Bioinformatic.zip`** in the upstream repository’s `data sets/` directory and copy it here.
